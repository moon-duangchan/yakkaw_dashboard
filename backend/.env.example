# Backend configuration
SERVER_PORT=8080
FRONTEND_ORIGINS=http://localhost:3000,https://your-ngrok-domain.ngrok.io
API_URL=https://yakkaw.mfu.ac.th/api/yakkaw/devices
QR_CONSUME_BASE_URL=http://localhost:8080
QR_DEFAULT_REDIRECT=http://localhost:3000/qr-create-device

# Cache (optional Redis)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASS=

# Database configuration
DB_HOST=postgres_db
DB_PORT=5432
DB_USER=yakkaw
DB_PASSWORD=1234
DB_NAME=yakkaw_db

# PostgreSQL container defaults (docker-compose)
POSTGRES_USER=${DB_USER}
POSTGRES_PASSWORD=${DB_PASSWORD}
POSTGRES_DB=${DB_NAME}

# Authentication (set to your secure value)
JWT_SECRET=replace-with-strong-secret
